Same stuff as we had in our original project 1 structure, just put it in here

Account creation and login

YOUR API GATEWAY ENDPOINT: '/api/users/'
PORT: 5002

Requirements:
1. log the user in and save the userId to local storage under 'userId'

2. Use a separate sqlite database initialized with the table 'users' including the column 'id' (INTEGER NOT NULL AUTOINCREMENT)

3. Display the username on the react component (UserService.jsx) and stylize with css. Feel free to format in the main App.jsx so it looks good.

Dependencies:
sqlite