A simple image service providing a few images from a database to app users. Maybe they can rotate through a few different images by pushing a button.

YOUR API GATEWAY ENDPOINT: '/api/images/'
PORT: 5002

Requirements:
1. Create a react component (ImageService.jsx) that will cycle through a few images at the push of a button, stored on a sqlite database, style with css (ImageService.css)



Dependencies:
sqlite